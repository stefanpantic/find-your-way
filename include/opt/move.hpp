#pragma once

namespace eRG::opt
{

	/*
	* @brief Enum containing eye point movemement options.
	*/
	enum class Move
	{
		forward,
		forwardx,
		forwardy,
		forwardz,
		strafe,
		strafex,
		strafey,
		strafez,
		up,
	}; /* enum class eRG::opt::Move */

}; /* namespace eRG::opt */
